<Window x:Class="Toplanti.Dialogs.AttendanceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Katilim Ekle" Height="550" Width="750"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Birim Secin (Coklu secim yapabilirsiniz):" FontSize="14" FontWeight="Bold" Margin="5"/>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="Tumunu Sec" x:Name="btnSelectAll" Click="BtnSelectAll_Click" Width="120" Height="30" Margin="5" FontSize="12"/>
            <Button Content="Tumunu Kaldir" x:Name="btnDeselectAll" Click="BtnDeselectAll_Click" Width="120" Height="30" Margin="5" FontSize="12"/>
        </StackPanel>
        
        <DataGrid Grid.Row="2" x:Name="dgUnits" AutoGenerateColumns="False" SelectionMode="Single" Margin="5" 
                  CanUserAddRows="False" CanUserDeleteRows="False" 
                  PreviewMouseDown="DgUnits_PreviewMouseDown" SelectionChanged="DgUnits_SelectionChanged">
            <DataGrid.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#87CEEB"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#B0E0E6"/>
            </DataGrid.Resources>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#87CEEB"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Sec" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                     HorizontalAlignment="Center" VerticalAlignment="Center"
                                     Click="CheckBox_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Birim No" Binding="{Binding Number}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Ad" Binding="{Binding FirstName}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Soyad" Binding="{Binding LastName}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Telefon" Binding="{Binding Phone}" Width="130" IsReadOnly="True"/>
                <DataGridTextColumn Header="Arsa Payi" Binding="{Binding LandShare, StringFormat=F2}" Width="100" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="Tamam" x:Name="btnOk" Click="BtnOk_Click" Width="100" Height="35" Margin="5" FontSize="14"/>
            <Button Content="Iptal" x:Name="btnCancel" Click="BtnCancel_Click" Width="100" Height="35" Margin="5" FontSize="14"/>
        </StackPanel>
    </Grid>
</Window>

